#ifndef SUM_H
#define SUM_H
/**
 * FILE: 4-sum.h
 * DESC: function-like macro SUM(x, y)
 *	that computes the sum of the numbers x and y.
 * @x: first argument to compute
 * @y: second argument to compute
 */

#define SUM(x, y) ((x) + (y))

#endif
