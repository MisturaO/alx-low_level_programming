#include <stdio.h>
#include "main.h"

/**
 * _strstr - gets the length of a prefix substring.
 * @haystack: param 1
 * needle: param
 * Return:string
 */

char *_strstr(char *haystack, char *needle)
{
	return (0);
}
