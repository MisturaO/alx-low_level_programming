#include <stdio.h>
#include "main.h"

/**
 * _strcpy - gets the length of a prefix substring.
 * @dest: param 1
 * src: param 2
 * Return: returns unsigned in 1
 */

char *_strcpy(char *dest, char *src)
{
	return (0);
}
