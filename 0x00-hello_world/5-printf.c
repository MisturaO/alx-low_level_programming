#include <stdio.h>

/**
 * main - The entry point of my code
 * Return: returns 0 means code runs without error
 * printf: prints a string followed by a newline
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
